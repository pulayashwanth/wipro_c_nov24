// as a group of integers can be stored in arrays 

// same group of characterts(ch) we will store in strings

// "\0" is called null character 

// single string will be terminated by "\0"

// "\0" and and "0" are different Ascii of "0"=48 and Ascii of "\0" = 0 

