

// arrays :   simply collection  of imilar data tyes is called arrays 

// array declaration :   datatype arrayname[length of the array]
  //eg : int a[30];

//   reading the elements from the array 

//  for( i=0 ;i < 29;i++){
//    printf("Enter marks : ");

//    scanf("%d", &marks[i])   //store data in array 
 
//
//       }


// length of the array :

// int length = *(&array + 1) - array;

// array = address of the  1st element in current array 

// &array + 1 = address of the next array 

// *(&array + 1) == address of thenet array 1st element 

// so by subracting the next next arrays first element with current arrays first array we will get the length 

// int length = sizeof(Arr) / sizeof(Arr[0]);

// sizeof(arr gives the entire array size )

// sizeof(arr[0])= gives the first element size 


